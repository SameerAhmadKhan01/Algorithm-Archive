Creation for C Program 

struct Node{
    int data; //could be of any data type 
    struct Node *next; //pointer to the next Node 
};

int create(){
   struct Node n;
   n.data=69;
   n.data=adress of next node;
}


Creating Class Of Node for C++ Program

class Node{
    int data;
    Node *next;
};

class Implementation{
    public:
      constructor 
      Destructor 
      CreateNode(){
        Node n=new Node();
        n.data=//entered val
        n.next= //adress of next node
      }
}